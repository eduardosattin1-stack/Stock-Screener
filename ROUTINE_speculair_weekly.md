# ROUTINE — "Speculair opus weekly" (canonical copy)

> **This file is the source of truth for the Claude Routines-UI entry "Speculair opus weekly"
> (Mondays ~12:00).** When the routine changes, change it HERE first and paste the body below into
> the Routines UI — the routine store is invisible to repo sessions, and text drift between the two
> caused the 2026-07-03 retirement detour. Everything between the BEGIN/END markers is the paste.
>
> Updated 2026-07-03: STEP 0 REPO SYNC added; STEP 3C DISRUPTOR LENS removed (retired 2026-07-02,
> FUTURE_RESOURCES_SPEC.md §10 — every `disruptor-*` mode is a code-level no-op since PR #4);
> STEP 3C is now the FUTURE RESOURCES universe build (Phase 1 scope; expands when Phases 2–3 ship).
>
> Updated 2026-07-10: Fable retired from every seat (pipeline-v3 plan, Week 1) — the Director,
> both skeptic kill-tiers, and B13's DeepDossier/CRO/Director all run on Opus now; `DIRECTOR_MODEL`/
> `SKEPTIC_MODEL` in weekly_opus_refresh.py flipped to `"opus"`, the published `engine` stamp is
> `opus-4.8` (not the fable hybrid). This is the second retirement (Fable was revived 2026-07-01
> after an earlier 2026-06-13 retirement) — this one is by explicit instruction, not an
> availability fallback, so references to "if Fable is ever retired again, fall back to opus" below
> are now moot (already done).
>
> Updated 2026-07-10 (later, same day — pipeline-v3 Week 2): TWO-TIER restructure live. STEP 2's
> workflow is now Radar → Coverage(Sonnet) → Delta(Sonnet, anchored seat updates) →
> Underwrite(Opus, ≤40 Tier-U cap, typed `valuation` block) → Gates(Haiku: coverage-merge +
> continuity-gate v2 + numeric-gate --enforce) → Skeptic(Opus, BEFORE the Director) → Director(Opus).
> STEP 2B's standalone skeptic is gap-fill only. Rollback: `SPECULAIR_TIERING=off`.
>
> Updated 2026-07-11: FUTURE RESOURCES Phases 2–3 SHIPPED (chain map + torque metrics + regime
> skeleton + the full Lane A debate → Director → post → publish chain: `fr-map`/`fr-map-merge`/
> `fr-prep`/`fr-numeric-gate`/`fr-input`/`fr-post`/`fr-csv`/`fr-publish` + `_fr_post.py` +
> `_resource_metrics.py`). STEP 3C below stays UNIVERSE-ONLY for now — folding the Lane A chain
> into this weekly auto-run (and its first `--gcs` publish, which needs the Phase-5 card) is
> BRUNO'S wiring decision, not an automatic expansion; until then the Lane A chain is launched
> by hand (see FUTURE_RESOURCES_SPEC.md §5/§7 + the fr-prep printout for the exact sequence).

<!-- ROUTINE BODY BEGIN -->

Weekly **all-Opus Speculair refresh**, designed to be fully hands-off. You are running automatically in the Stock-Screener repo at `C:\Users\Bruno\Stock-Screener` with NO memory of prior conversations — everything you need is below and in the repo. Goal: re-debate the entire per-methodology candidate universe with Claude Opus 4.8 (regime-aware), re-pick the apex 10, and publish the updated debates + per-symbol debate HISTORY to production GCS so every stock page + the Speculair tracker show fresh Opus analysis. The debate runs as Claude Code Opus SUBAGENTS on the subscription — no Anthropic/OpenAI API key (only FMP + GCS + web calls use keys/network, already configured).

**Operating rules (Bruno's standing instructions):**
- This pipeline runs ENTIRELY on Claude Code (local). NEVER deploy any of it to Cloud Run / Cloud Run Jobs.
- The frontend is already deployed and reads GCS live, so NO frontend commit/deploy is needed — refreshing GCS data IS the whole job.
- This routine ONLY consumes the production scan's outputs and overlays the Opus debate layer. Do NOT edit `screener_v6.py`, the production scan, or any Cloud Run job.
- All commands below are pre-allowlisted — every `python backend/weekly_opus_refresh.py …` mode (via a wildcard rule), `publish_to_frontend.py --gcs`, `compact_table.py`, and `gcloud storage …` — so a headless run completes without permission prompts. The value-director subagent (STEP 3B) only Reads/Writes local files. If a step's GUARD trips, STOP and report rather than publishing degraded data.
- The DISRUPTOR LENS is RETIRED (2026-07-02, FUTURE_RESOURCES_SPEC.md §10): never run any `disruptor-*` mode — they are code-level no-ops that print a RETIRED notice and exit 0 (`ALLOW_DISRUPTOR=1` is the only override, operator-only).

STEP 0 — REPO SYNC (~30 s; keeps this box's code current — the repo is developed both here and in cloud sessions, and code fixes land on `main` via PRs). Run `git rev-parse --abbrev-ref HEAD` and `git status --porcelain`. ONLY if the branch is `main` AND the tree is clean, run `git pull --ff-only origin main` and report the old→new commit in the STEP 4 summary. In ANY other state — wrong branch, dirty tree, diverged history, a permission prompt on the git command — print `REPO SYNC SKIPPED: <reason> — running with the local copy` and CONTINUE; NEVER stash, reset, force-pull, or switch branches unattended. A skipped sync is never a failure.

STEP 1 — PREP (deterministic, ~3-6 min; fetches FMP transcripts). Run:
`python backend/weekly_opus_refresh.py prep`
It (a) **self-cleans** — archives the prior run's `results_regime/`, `dossiers/`, `apex_basket_opus_regime.json` into `_opus_debate/_archive_prev/` so the Director only ever sees ONE coherent pass; (b) sources the universe from the **RAW 11-methodology production screen** (`scans/methodology_picks.json` on GCS — the full opportunity set, re-read every week; NOT the curated `speculair_baskets.json`, which created a self-shrinking loop), and unions in the current apex (held names); (c) builds per-name input bundles + fetches the latest FMP transcripts; (d) for names with NO FMP transcript, marks them for **ONLINE fetch** instead of skipping; (e) emits a self-contained debate workflow with both name-lists baked in. It prints two key lines:
  `raw screen methodology_picks.json: last_updated=... methodologies=<M>`
  `PREP OK: <fmp> with FMP transcripts + <online> via online fetch = <TOTAL> total candidates (online: [...])` and `WORKFLOW_SCRIPT=<absolute path>`.
CHANGE-DETECTION (2026-07-01): most unchanged names are now CARRIED FORWARD (prior debate restamped with live price + as_of + `carried=true`) instead of re-debated — a name re-debates only on: new transcript / >10% price move / prior catalyst PENDING_HARD / seat-relevant (apex+runner in any book, ALWAYS) / record >21d old / new to the universe. Expect ~45-70 debates instead of ~149; earnings weeks spike back up by design. `FORCE_FULL_DEBATE=1` env overrides the gate.
GUARD: if `COVERED < 30` (debated + carried, the last number on the PREP OK line), the printed `methodologies=<M>` is `< 8`, or the WORKFLOW_SCRIPT path is missing → STOP and report a degraded screen (cf. the 2026-06-06 incident: 8 methodologies / 15 names). A healthy run COVERS ~45-150 across 11+ methodologies; the DEBATED subset alone may legitimately be small on a quiet week.

STEP 1B — B13 CATALYST DEBATE (Basket-13's own weekly re-underwrite; ~15-25 min; OPTIONAL — skip silently if no candidates). **B13-SEPARATION RULE (2026-07-08, Bruno's directive after the 07-07 forensics): the Basket-13 catalyst book is FULLY SEPARATE from the apex.** Its catalyst-framed debates are NEVER seeded into `results_regime/` and the apex Director may NOT seat any equity special-sit (`catalyst-seed` is a code-level no-op that prints RETIRED; the Director's STEP-3b is now a hard prohibition — the old seeding let a price-blind regime re-debate OVERWRITE the correctly-priced catalyst record: the AAUC fabricated-price incident, plus the FIP A/5→B/3 two-rubrics-one-file whiplash). This step now exists ONLY to refresh B13's own artifacts. Run:
`python backend/weekly_opus_refresh.py catalyst-prep`
It regenerates the catalyst debate workflow over the B13 book (candidates + held seats), printing `catalyst-prep: <N> B13 names -> [...]` + `WORKFLOW_SCRIPT=<path>`. If it prints `no non-binary B13 equity special-sits` → SKIP this step. Otherwise invoke the Workflow tool: `Workflow({ scriptPath: "<the WORKFLOW_SCRIPT path>" })` (Interrogator → Architect → CRO + Skeptic + Director, catalyst-framed, batched ≤6). Do NOT run `catalyst-seed` (retired no-op). ORDERING NOTE: the `_catalyst_results/` + `_catalyst_skeptic/` artifacts are the Basket-13 entry gate + resolution radar (`_basket13_gen` joins them; `_basket13_inject.validate` hard-rejects a fresh REFUTED/FIRED; `_basket13_mark` flags dead seats) — so this catalyst Workflow must run BEFORE any B13 re-debate that week (the `basket13-redebate` Saturday schedule); artifacts older than 10 days downgrade the gate to warn-only. GUARD: this lane is additive — if anything here fails, SKIP it and proceed to STEP 2; the apex compounder book is never blocked by it.

STEP 2 — TWO-TIER WORKFLOW (Radar → Coverage → Delta → Underwrite → Gates → Skeptic → Director; ~20-40 min steady-state — Week 2 restructure, 2026-07-10). Invoke the Workflow tool: `Workflow({ scriptPath: "<the WORKFLOW_SCRIPT path from Step 1>" })`. PREP already partitioned the universe (its `tier-select:` line shows the funnel — GUARD: if it printed `GUARD tier-select: Tier-U only <n> names (<15)`, STOP and report a thin week): **Tier-U** (hard cap 40 = held seats in DELTA mode + triggered/intake names in FULL Opus debates) gets underwritten; **Tier-C** gets a Sonnet coverage refresh (verdict/conviction INHERITED by code — the agent schema has no grade fields) or a deterministic carry restamp. B13 names are tier-excluded (underwritten only in the catalyst workflow). The workflow phases:
- **Coverage** (Sonnet): structured updates -> `_coverage_updates/`, merged by the Gates phase.
- **Delta** (Sonnet): each held seat updates its OWN anchor record (`_archive_prev/`); may move prices/status freely, sop/conviction(±1) ONLY with a dated fact recorded in `deltas[]`; may NOT flip the verdict (sets `escalate_full_debate` instead — next cycle's T6 trigger). The churn fix: unjustified changes are REVERTED deterministically.
- **Underwrite** (Opus): full debates — same Interrogator→Architect→CRO pass as before, now ALSO emitting the TYPED `valuation` block (bear_px/base_fv_px/bull_px point numbers; agents state LEVELS, the pipeline computes every ratio) + `last_full_debate` stamp. Online names fetch transcripts FMP-first.
- **Gates** (Haiku runner): `coverage-merge` (inherit grades verbatim) → `continuity-gate` (v2: flags full-record flips, REVERTS unjustified delta changes) → `numeric-gate --legacy --enforce` (ENFORCED since 2026-07-11 after the clean calibration cycle: REJECT/EXCLUDE stamp records, diverging prose R:R is rewritten w/ _orig kept, and _regime_post demotes gated names from seats). If regime-post later prints a skeptic-coverage WARN (a workflow skeptic returned DONE without writing its shard — seen 2026-07-11), gap-fill: run `regime-skeptic`, trim the generated workflow SYMS to the missing names, run it, re-run `regime-post`. Quote all three summary lines in the final report.
- **Skeptic** (Opus, BEFORE the Director — new): kill-tier over all of Tier-U; conviction≤2 non-seats self-skip cheaply; shards land in `_skeptic_regime/` where the Director reads them PRE-seat (a REFUTED is a hard no-seat, cited in the memo — demotion evidence now informs seating instead of vaporizing seats after the fact).
- **Director** (Opus): unchanged rubric + reads the skeptic shards + continuity flags. Writes `apex_basket_opus_regime.json`.
ROLLBACK LEVER: `SPECULAIR_TIERING=off` before prep restores the legacy single-tier flow (every debate name full-Opus, empty Delta/Coverage/Skeptic phases; run STEP 2B's standalone skeptic as before). RATE-LIMIT POLICY unchanged: transient server limit → resume once; HARD session limit → STOP until the printed reset, THEN resume (cached agents replay free).
ROBUSTNESS: run `python backend/_opus_debate/compact_table.py results_regime` to check coverage. Proceed if `apex_basket_opus_regime.json` has ~10 picks and most Tier-U names debated; if missing/thin, re-invoke the SAME Workflow once (cached agents return instantly).

STEP 2B — REGIME SKEPTIC (GAP-FILL ONLY since the Week 2 restructure — the kill-tier now runs INSIDE the STEP 2 workflow, before the Director). Run this standalone verb ONLY if the workflow's Skeptic phase failed or left gaps (check `_skeptic_regime/` shard count vs the Tier-U count):
`python backend/weekly_opus_refresh.py regime-skeptic` → Workflow the printed path.
ALWAYS run the deterministic post layer:
`python backend/weekly_opus_refresh.py regime-post`
(the backstop: consumes the skeptic verdicts — a REFUTED apex member that somehow got seated is DEMOTED to runner-ups [skeptic-has-teeth] — and applies the moat-erosion + secular-theme caps; same-run pre-Director shards are FRESH under the 24h window, not stale). GUARD: if regime-post leaves <8 apex names, re-check the skeptic shards before publishing; the demotions are the design working, not an error.

STEP 3 — PUBLISH TO PRODUCTION GCS (idempotent). Run:
`python backend/_opus_debate/publish_to_frontend.py --gcs`
This (a) **rebuilds `per_methodology_baskets`** from the raw screen filtered to names DEBATED this run, then overlays each one's Opus debate (so every debated stock page shows fresh analysis); (b) swaps in the new apex + updates the apex track record (chained NAV, rotation log) via `_update_apex_tracking`; (c) **appends this run's debate to each name's per-symbol dated HISTORY**; (d) pushes THREE things to `gs://screener-signals-carbonbridge/scans/`: `speculair_baskets.json`, `speculair_apex_tracking.json`, and the `speculair_debate_history/` dir. Confirm the output shows:
  `rebuilt per_methodology_baskets ... <~134> debated picks` and `overlaid with Opus debate: <~134> (no Opus debate yet: [])`
  `GCS push scans/speculair_baskets.json: OK`, `... speculair_apex_tracking.json: OK`, and `... speculair_debate_history/: OK`
If any push shows FAILED, re-run the SAME command once — it is idempotent (history dedups by run-date; tracking refreshes from GCS first; one publish = one rotation, so never publish twice in a run). If it prints `apex_basket_opus_regime.json has no picks` it aborts — go back and fix STEP 2.

STEP 3B — VALUE LENS (pure-value re-grade of the SAME debate; regime overlay stripped; ~6-8 min). The value book REUSES this run's `results_regime/` — NO new debate. Run:
`python backend/weekly_opus_refresh.py value-input`
It builds `value_grade_input.json` (per debated name: 5-method MoS spread, the CRO-normalized `sop_mos_pct`, the catalyst-blind `value_conviction`, funded-leverage solvency = net-funded-debt/EBITDA + interest coverage [FMP /stable/ ratios; float/reserves netted out], cyclical-peak + TTM-vs-FY-freshness flags, the forensic gate, peer verdict), PRINTS the weekly funnel-quality stats (Spearman scan-vs-CRO, collapse/artifact rates, cross-lens rescues), updates `forensic_ledger.json` (EXCLUDE names get short re-checks next prep, not full debates), and writes `value_director_prompt.txt` (the hardened pure-value rubric, incl. the hidden-factor correlation stress + prior-run measured correlations). Then invoke ONE Director subagent (Agent tool, `general-purpose`, `model: opus` — Fable retired from the Director seat 2026-07-10, pipeline-v3 Week 1), told exactly: "Read backend/_opus_debate/value_director_prompt.txt IN FULL and execute it over backend/_opus_debate/value_grade_input.json; write backend/_opus_debate/apex_basket_value.json EXACTLY per its schema; reply DONE." It applies the four robustness fixes (system-of-record: CRO `sop_fair_value` over the raw scan MoS; cyclical-peak normalization; forensic-credibility gate; funded-leverage solvency) AND the hidden-factor correlation stress (diversify or size-cap any ≥2 hidden cluster), emitting the value apex 10 with `exposure_axes`, numeric `size_units`/`combined_caps`/`thesis_break_px`/`bear_fv_px`, and a BEAR REBUTTAL memo subsection.
THEN the SKEPTIC kill-tier (independent adversarial pass — the in-context CRO shares the bull's activations; this one sees ONLY the bear side + live web): run
`python backend/weekly_opus_refresh.py value-skeptic`
and invoke the Workflow tool on the printed SKEPTIC_WORKFLOW path (~16 Opus agents — the seat's model is set by SKEPTIC_MODEL in weekly_opus_refresh.py — default-REFUTED unless primary sources confirm). A REFUTED apex member is DEMOTED to runner-ups by the post layer (skeptic-has-teeth — Bruno's standing decision). Then run, in order:
`python backend/weekly_opus_refresh.py value-post`  (DETERMINISTIC safety post-layer — runs BEFORE the CSV/publish: half-sizes CRO-only legs [<=2/5 model MoS] + stale anchors [stale + peak + FIRED]; builds the weight vector incl. the Director's combined_caps; market-based stress [basket to 52w-lows and to -15% recession]; a MEASURED 2y weekly-return correlation matrix [caps any >=0.7 pair >16% combined weight]; thesis-break exits; and cross-surface gate_sync [EXCLUDE names stripped from the regime apex too]. Idempotent.)
`python backend/weekly_opus_refresh.py value-csv`  (per-apex CSV + memo)
`python backend/weekly_opus_refresh.py baskets-csv`  (one CSV: every basket membership × the full debate)
`python backend/weekly_opus_refresh.py value-publish --gcs`
value-publish stages `frontend/public/speculair_value_apex.json` (apex + runner_ups + memo + exposure_axes + the value NAV summary), CHAINS the value book's OWN live-forward NAV (rotating on this week's re-grade) via `_update_apex_tracking` into the SEPARATE state file `speculair_value_tracking.json`, and pushes the value apex + its tracking to `gs://screener-signals-carbonbridge/scans/`. Confirm: `value_publish: 10 apex + N runners | tracking nav=... since=...%` and `GCS push scans/speculair_value_apex.json: OK`.
GUARD: if `apex_basket_value.json` has <8 apex names or the GCS push FAILS, re-run `value-publish --gcs` once; if still failing, report it (the regime/catalyst book from STEP 3 is independent and already live).

STEP 3C — FUTURE RESOURCES UNIVERSE (the basket that replaced the retired Disruptor Lens; THIS STEP IS UNIVERSE-BUILD ONLY — no debate, NO publish; ~3-8 min when it runs, most weeks it skips). The Future Resources basket covers six physical-world value chains (uranium fuel cycle, copper/electrification, rare earths & strategic metals, power-for-AI, robotics, quantum) under the physical-anchor rule — see FUTURE_RESOURCES_SPEC.md. STATUS (2026-07-11): spec Phases 2–3 are BUILT — the monthly chain map (`fr-map` emit → Workflow → `fr-map-merge`), the torque metrics (`_resource_metrics.py`), the regime skeleton (FUTURE_RESOURCES_REGIME.md + `future_resources/regime_state.json`), and the full Lane A chain (`fr-prep` → debate Workflow → `fr-numeric-gate --enforce` → `fr-input` → FR Director agent → `fr-post` → `fr-csv` → `fr-publish`) all exist as `fr-*` modes. They are DELIBERATELY NOT part of this weekly auto-run: wiring the Lane A chain into the routine (and the maiden `fr-publish --gcs`, which waits for the Phase-5 card) is Bruno's decision. Do NOT run any fr-* mode beyond `fr-universe` in this routine until the runbook says so.
CADENCE CHECK first: read `backend/_opus_debate/future_resources/_candidates.json` — if it exists and its `built_at` is less than 21 days old, SKIP this step (report "FR universe fresh, skipped"). Otherwise run:
`python backend/weekly_opus_refresh.py fr-universe`
It screens the six chains via FMP company-screener across NYSE/NASDAQ/AMEX (AMEX carries the uranium cohort) and applies the two-lane gates — Lane A producers/royalties (TTM FCF>0 OR OCF>0, TTM EBITDA>0, funded solvency != weak, mcap>=$500M) and Lane B developers (mcap>=$150M, no profitability gate, cash-runway fields stamped for the funded-through-milestone gate). It prints per-chain `  Stage A [<chain>]: <n> unique candidates` lines and ends with `FR UNIVERSE STAGE A+B OK: screened=<n> liquid=<n> lane_a=<n> lane_b=<n>` + a `by_chain x lane` map.
FIRST-RUN CHECKLIST (one time, spec §11): (a) any `WARN [...] industry '...' returned 0 rows` line on a chain that should have members means a misspelled/(verify) FMP industry string — fix it in `backend/_opus_debate/future_resources_chains.json` (config only, never code) and re-run; (b) spot-check the anchors landed: CCJ/UEC/UUUU (uranium — the AMEX canary), FCX/SCCO (copper), MP (rare earths), GEV/CEG (power), ISRG/TER-class (robotics), IONQ/RGTI-class present in lane_b (quantum, pre-FCF by design); V/JPM/TSLA-class absent everywhere.
GUARD: any `GUARD: ... STOP` line (no FMP_API_KEY / screen returned <100 raw rows / `uranium_fuel_cycle mapped 0 candidates` AMEX canary) → report it and SKIP this step; the Apex + Value books are already live and completely unaffected. NOTHING is published from this step — no GCS push, no NAV, no card; it only stages `future_resources/_candidates.json` for the upcoming debate phases. (LEGACY NOTE: the old STEP 3C ran the DISRUPTOR LENS — retired 2026-07-02; every `disruptor-*` mode now prints a RETIRED notice and exits 0. Do not run them; if one is ever needed forensically, the operator sets `ALLOW_DISRUPTOR=1`.)

STEP 4 — VERIFY + REPORT. Publish already round-trips: its final `LIVE readback (fresh): apex=[...] engine=... per_methodology_baskets=N` line reads the file back FROM GCS via `gcloud storage cat` (fresh — the public-URL read can return a stale cache right after a write), confirming what is actually live. Confirm it shows the new 10-name apex and `engine=opus-4.8` (all-Opus again since Fable's 2026-07-10 retirement from the Director/Skeptic seats — pipeline-v3 Week 1). Then report a concise summary: the new apex 10 with director convictions, how it changed vs last week (call out any ONLINE-fetched names that made the basket), how many names debated (FMP vs online), and any failures/caveats; ALSO report the VALUE LENS apex 10 (value_score + funded_solvency + how the correlation stress resolved any ≥2 hidden cluster) and which names sit in BOTH the regime and value apex (the cross-lens convictions). If STEP 0 pulled, state the old→new commit; if STEP 3C ran, also report the Future Resources funnel (per-chain lane_a/lane_b counts + any 0-row industry WARNs). Keep figures grounded — never fabricate numbers.

NOTES: CATALYST_WATCH_REGIME.md is read live each run, so the tilt always reflects the latest monthly regime. No FMP transcript for a name is no longer a skip — it is fetched online (never faked; if truly nothing is found, the agent reasons from the bundled fundamentals and says so). Each weekly run appends one dated entry to every debated name's history (capped at 12), so the stock-page history dropdown grows over time with no extra action.

DISRUPTOR RETIREMENT + FUTURE RESOURCES: the Disruptor Lens was retired 2026-07-02 (the Visa drift — FUTURE_RESOURCES_SPEC.md §10). Its frontend card is a frozen record ("Final track record · retired") and must never receive fresh data — enforced in code: every `disruptor-*` mode is a no-op. The Future Resources basket replaces it; its nightly NAV tuple is already pre-staged in the production scan's `_mark_speculair_nav()` and self-no-ops ("skipped — no published constituents") until the first `fr-publish --gcs` lands the payload. As of 2026-07-11 the Phase 2–3 chain (chain map, torque metrics, regime skeleton, Lane A debate → Director → post → publish) is BUILT and launched by hand; expanding this routine's STEP 3C to run it weekly — and the maiden GCS publish itself, which waits for the Phase-5 card — are Bruno's wiring decisions. Note Lane A has NO skeptic tier by design (spec §5); the adversarial teeth are the Interrogator credibility gate + the numeric gate + the deterministic post caps.

VALUE LENS + DAILY NAV split: the same debate is ALSO graded on a pure-value rubric (catalyst overlay stripped) with funded-leverage solvency + a hidden-factor correlation stress → a second tracked book (the blue "Value Lens" card beside the green Apex). This weekly skill RE-GRADES the constituents of both books and publishes them; the DAILY mark-to-market of BOTH NAVs (so the sparklines draw every night, not just weekly) is handled by the production nightly scan's `_mark_speculair_nav()` in the deployed `screener-sp500` job — so this routine never marks daily and still never edits or deploys the Cloud Run scan. See the repo memory `project_speculair_opus_pipeline.md` for the full architecture.

<!-- ROUTINE BODY END -->
